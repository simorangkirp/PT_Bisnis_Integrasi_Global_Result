name: big_test
description: A new Flutter project.
publish_to: 'none'


version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"


dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^7.3.3
  flutter_dropzone: ^3.0.0
  flutter_svg: ^0.22.0
  # http: ^0.13.
  http:
    git:
      url: https://github.com/simorangkirp/http-little-changes
      path: pkgs/http
  shared_preferences: ^2.0.7

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
flutter:

  uses-material-design: true